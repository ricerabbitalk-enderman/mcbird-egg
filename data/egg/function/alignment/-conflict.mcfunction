scoreboard players operation #egg:alignment/-conflict|relationship -- = #egg:alignment/-conflict|<<alignment --
scoreboard players operation #egg:alignment/-conflict|relationship -- *= @s egg.alignment
execute if score #egg:alignment/-conflict|relationship -- matches ..-1 run return 1
return 0
